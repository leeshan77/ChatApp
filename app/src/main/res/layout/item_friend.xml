<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/item_friend_imageview"
        android:src="@drawable/baseline_account_circle_24"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/item_friend_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:text="nickname"
        android:textSize="18sp"
        app:layout_constraintStart_toEndOf="@id/item_friend_imageview"
        app:layout_constraintTop_toTopOf="@id/item_friend_imageview"
        app:layout_constraintBottom_toBottomOf="@id/item_friend_imageview"
        />

</androidx.constraintlayout.widget.ConstraintLayout>